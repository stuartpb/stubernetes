- op: add
  path: /spec/values/redis
  value:
    enabled: false
    # master:
    #   persistence:
    #     storageClass: throwaway
    #   nodeSelector:
    #     kubernetes.io/arch: amd64
    # replica:
    #   persistence:
    #     storageClass: throwaway
    #   nodeSelector:
    #     kubernetes.io/arch: amd64
