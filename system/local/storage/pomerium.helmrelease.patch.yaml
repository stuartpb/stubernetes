- op: add
  path: /spec/values/redis
  value:
    enabled: true
    image:
      repository: library/redis
      tag: 6.2.5-alpine
    master:
      command: [redis-server]
      args: []
      persistence:
        storageClass: throwaway
#      nodeSelector:
#        kubernetes.io/arch: amd64
    replica:
      command: [redis-server]
      args: []
      persistence:
        storageClass: throwaway
#      nodeSelector:
#        kubernetes.io/arch: amd64
